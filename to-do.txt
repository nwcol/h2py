General
- make bin conventions uniform! arrays or 2-tuples?
- make sure bootstrapping works: CIs look big?

Matrices
- cleaner handling of missing data/mismatching alternate alleles
- dynamic masking and access

Parsing
- check end behavior (largest/smallest bin) to make sure no errors are biasing
these; they often look a bit odd in simulation, maybe due to fewer observations
- insure the pairwise/multi-sample between-diploid estimators are equivalent
to numerical accuracy
- integrating GP array coverage with mask/interval when parsing with GPs
(reorganize denominator computation). What is the best way to deal with the
likely different coverages among samples?
- add verbosity about mask coverage and denominator computation (number of site
pairs, number of sites, map range)
- non-destructive masking of variation matrices
- mutation map reader and scaling.
- implement multi-sample estimators.

Plotting
- document/delineate clearly between the single and arbitrary comparison
plotting funcs
- refine single-comparison plotting function
- write arbitrary-comparison plotting function

Simulations
- Document and clean up
- what's the best way to estimate genotype priors from data? Uniform priors
are unacceptable.
    - how much does this affect estimated H2?

Uncerts
- fix the LRT adjustment, which currently returns negative values in my IM
example.

Tests

Examples/notebooks
- genotype parsing/bootstrapping/inference/uncerts example
- GP example with analogous pipeline and same model/params; with --depth param
- multi-sample GP example?
